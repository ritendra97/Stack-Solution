import java.util.Stack;
// Evaluation of postfix expression

public class Main4 {

    static Stack<Integer> st = new Stack<>();
    public static void push_Element(int x){

        if(st.isEmpty()){
            return;
        }
        int temp = st.pop();
        push_Element(temp);
        st.push(x);
    }

    public static void main(String [] args){
        st.push(5);
        st.push(4);
        st.push(3);
        st.push(2);
        st.push(1);
        push_Element(7);
        while (!st.isEmpty()){
            System.out.println(st.pop());
        }
    }
}
